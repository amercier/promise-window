{
  "private": true,
  "scripts": {
    "list": "find tests -maxdepth 1 -type f -name *.html | while read f; do basename \"$f\" .html; done | sed 's/^test-//'",
    "tdd": "./node_modules/karma/bin/karma start",
    "test": "for suite in $(npm run -s list); do SUITE=$suite CI=true npm run tdd || exit 1; done"
  },
  "devDependencies": {
    "bower": "^1.3.12",
    "karma": "^0.12.31",
    "karma-chrome-launcher": "^0.1.7",
    "karma-phantomjs-launcher": "^0.1.4",
    "karma-qunit": "^0.1.4",
    "karma-sinon": "^1.0.4",
    "qunitjs": "^1.17.1",
    "sinon": "^1.12.2"
  }
}
